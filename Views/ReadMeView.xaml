<UserControl x:Class="EconToolbox.Desktop.Views.ReadMeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="clr-namespace:MdXaml;assembly=MdXaml">
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0"
                Background="{StaticResource Brush.Surface}"
                BorderBrush="{StaticResource Brush.Border}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="{StaticResource Padding.Section}">
            <md:MarkdownScrollViewer x:Name="MarkdownViewer"
                                     Markdown="{Binding MarkdownContent}"
                                     VerticalScrollBarVisibility="Auto"
                                     HorizontalScrollBarVisibility="Disabled"
                                     Background="Transparent"
                                     Foreground="{StaticResource Brush.TextPrimary}"
                                     FontSize="14"/>
        </Border>
        <StackPanel Grid.Row="1" Margin="0,16,0,0">
            <Border Style="{StaticResource Border.ResultInfo}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="" FontSize="18" Foreground="{StaticResource Brush.Primary}" Margin="0,0,8,0"/>
                        <TextBlock Text="How to Read the Results" FontWeight="Bold" Foreground="{StaticResource Brush.Primary}"/>
                    </StackPanel>
                    <TextBlock TextWrapping="Wrap" Margin="0,6,0,0">
                        The rendered README mirrors the repository markdown, so headings, links, and code snippets appear exactly as authored. Use the built-in scroll and search (Ctrl+F) to navigate setup steps while staying inside the toolbox.
                    </TextBlock>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Border.ResultInfo}" Margin="0,12,0,0">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="" FontSize="18" Foreground="{StaticResource Brush.Primary}" Margin="0,0,8,0"/>
                        <TextBlock Text="Policy Notes" FontWeight="Bold" Foreground="{StaticResource Brush.Primary}"/>
                    </StackPanel>
                    <TextBlock TextWrapping="Wrap" Margin="0,6,0,0">
                        <Run Text="• Treat this tab as a convenience copy—retain the signed project README and governance documents in the source repository of record."/>
                        <LineBreak/>
                        <Run Text="• Verify hyperlinks before sharing externally; some resources may be limited to CAC or MSC networks."/>
                        <LineBreak/>
                        <Run Text="• Reference the README when onboarding team members so processes stay aligned with district quality management plans."/>
                    </TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
