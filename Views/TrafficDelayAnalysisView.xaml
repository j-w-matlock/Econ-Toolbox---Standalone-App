<UserControl x:Class="EconToolbox.Desktop.Views.TrafficDelayAnalysisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:EconToolbox.Desktop.ViewModels"
             mc:Ignorable="d"
             Background="{DynamicResource App.SurfaceAlt}"
             d:DataContext="{d:DesignInstance Type=vm:TrafficDelayAnalysisViewModel}">
    <UserControl.Resources>
        <Style TargetType="TextBox" BasedOn="{StaticResource Form.TextBox}"/>
    </UserControl.Resources>

    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Padding="{StaticResource Padding.Card}">
        <StackPanel Style="{StaticResource Layout.ContentStack}">
            <Border Style="{StaticResource Border.Explainer}">
                <StackPanel>
                    <TextBlock Text="Traffic Delay Analysis (USACE Policy)"
                               Style="{StaticResource Text.SectionTitleAccent}"/>
                    <TextBlock Margin="{StaticResource Margin.TopSmall}"
                               Style="{StaticResource Text.Body}"
                               TextWrapping="Wrap"
                               Text="This module reproduces the provided traffic detour and delay-impact workbook logic for flood routing scenarios, including operating costs, value-of-time adjustments, and total traffic impacts."/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Border.ResultInfo}">
                <Grid Grid.IsSharedSizeScope="True">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                        <ColumnDefinition Width="170"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Duration of flooding (days)" Grid.Row="0" Margin="0,0,10,8"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding DurationOfFloodingDays, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="Traffic count for flooding period" Grid.Row="1" Margin="0,0,10,8"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding TrafficCountForFloodingPeriod, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="Miles in original route" Grid.Row="2" Margin="0,0,10,8"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding OriginalRouteMiles, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="Alternative route mileage" Grid.Row="3" Margin="0,0,10,8"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding AlternativeRouteMiles, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="Total passengers" Grid.Row="4" Margin="0,0,10,8"/>
                    <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding TotalPassengers, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="Operating cost per mile" Grid.Row="5" Margin="0,0,10,8"/>
                    <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding OperatingCostPerMile, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="Median household income" Grid.Row="6" Margin="0,0,10,8"/>
                    <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding MedianHouseholdIncome, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="AEP that causes delay (optional)" Grid.Row="7" Margin="0,0,10,8"/>
                    <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding AepThatCausesDelay, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="Discount rate % (optional)" Grid.Row="8" Margin="0,0,10,8"/>
                    <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding DiscountRatePercent, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="Period of analysis (years, optional)" Grid.Row="9" Margin="0,0,10,0"/>
                    <TextBox Grid.Row="9" Grid.Column="1" Text="{Binding AnalysisPeriodYears, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>
            </Border>

            <Border Style="{StaticResource Border.ResultInfo}">
                <StackPanel>
                    <TextBlock Text="Detour cost calculations" Style="{StaticResource Text.SectionTitle}"/>
                    <TextBlock Margin="0,8,0,0" Text="Additional miles per vehicle"/>
                    <TextBlock Style="{StaticResource Text.BodyStrong}" Text="{Binding AdditionalMilesPerVehicle, StringFormat={}{0:N2}}"/>
                    <TextBlock Margin="0,8,0,0" Text="Total additional mileage"/>
                    <TextBlock Style="{StaticResource Text.BodyStrong}" Text="{Binding TotalAdditionalMileage, StringFormat={}{0:N2}}"/>
                    <TextBlock Margin="0,8,0,0" Text="Additional operating costs per day"/>
                    <TextBlock Style="{StaticResource Text.BodyStrong}" Text="{Binding AdditionalOperatingCostsPerDay, StringFormat={}{0:C2}}"/>
                    <TextBlock Margin="0,8,0,0" Text="Total additional detour costs"/>
                    <TextBlock Style="{StaticResource Text.BodyStrong}" Text="{Binding TotalAdditionalDetourCosts, StringFormat={}{0:C2}}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Border.ResultInfo}">
                <StackPanel>
                    <TextBlock Text="Value of time and delay impacts" Style="{StaticResource Text.SectionTitle}"/>
                    <TextBlock Margin="0,8,0,0" Text="Hourly income"/>
                    <TextBlock Style="{StaticResource Text.BodyStrong}" Text="{Binding HourlyIncome, StringFormat={}{0:C2}}"/>
                    <TextBlock Margin="0,10,0,0" Text="Low time savings (0-5 min): Work / Social / Other"/>
                    <TextBlock Style="{StaticResource Text.BodyStrong}" Text="{Binding LowTimeSavingsWorkTripsValue, StringFormat={}{0:C2}}"/>
                    <TextBlock Style="{StaticResource Text.BodyStrong}" Text="{Binding LowTimeSavingsSocialRecreationValue, StringFormat={}{0:C2}}"/>
                    <TextBlock Style="{StaticResource Text.BodyStrong}" Text="{Binding LowTimeSavingsOtherValue, StringFormat={}{0:C4}}"/>
                    <TextBlock Margin="0,10,0,0" Text="Medium time savings (6-15 min): Work / Social / Other"/>
                    <TextBlock Style="{StaticResource Text.BodyStrong}" Text="{Binding MediumTimeSavingsWorkTripsValue, StringFormat={}{0:C2}}"/>
                    <TextBlock Style="{StaticResource Text.BodyStrong}" Text="{Binding MediumTimeSavingsSocialRecreationValue, StringFormat={}{0:C2}}"/>
                    <TextBlock Style="{StaticResource Text.BodyStrong}" Text="{Binding MediumTimeSavingsOtherValue, StringFormat={}{0:C2}}"/>
                    <TextBlock Margin="0,10,0,0" Text="High time savings (&gt;15 min): Work / Social / Other"/>
                    <TextBlock Style="{StaticResource Text.BodyStrong}" Text="{Binding HighTimeSavingsWorkTripsValue, StringFormat={}{0:C2}}"/>
                    <TextBlock Style="{StaticResource Text.BodyStrong}" Text="{Binding HighTimeSavingsSocialRecreationValue, StringFormat={}{0:C2}}"/>
                    <TextBlock Style="{StaticResource Text.BodyStrong}" Text="{Binding HighTimeSavingsOtherValue, StringFormat={}{0:C2}}"/>
                    <TextBlock Margin="0,10,0,0" Text="Delay impacts per day"/>
                    <TextBlock Style="{StaticResource Text.BodyStrong}" Text="{Binding DelayImpactsPerDay, StringFormat={}{0:C2}}"/>
                    <TextBlock Margin="0,8,0,0" Text="Total delay impacts"/>
                    <TextBlock Style="{StaticResource Text.BodyStrong}" Text="{Binding TotalDelayImpacts, StringFormat={}{0:C2}}"/>
                    <TextBlock Margin="0,8,0,0" Text="Total traffic impacts"/>
                    <TextBlock Style="{StaticResource Text.SectionTitleAccent}" Text="{Binding TotalTrafficImpacts, StringFormat={}{0:C2}}"/>
                    <TextBlock Margin="0,10,0,0" Text="Annualized traffic impacts (AEP-adjusted)"/>
                    <TextBlock Style="{StaticResource Text.BodyStrong}" Text="{Binding AnnualizedTrafficImpacts, StringFormat={}{0:C2}}"/>
                    <TextBlock Margin="0,8,0,0" Text="Discounted total over analysis period"/>
                    <TextBlock Style="{StaticResource Text.SectionTitleAccent}" Text="{Binding DiscountedTrafficImpactsOverAnalysisPeriod, StringFormat={}{0:C2}}"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
