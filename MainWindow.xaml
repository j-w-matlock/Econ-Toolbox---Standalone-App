<Window x:Class="EconToolbox.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:EconToolbox.Desktop.ViewModels"
        xmlns:views="clr-namespace:EconToolbox.Desktop.Views"
        Title="Economic Toolbox"
        Height="700"
        Width="960"
        MinWidth="640"
        MinHeight="600"
        ResizeMode="CanResize"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True"
        ToolTipService.InitialShowDelay="200"
        ToolTipService.ShowDuration="20000"
        ToolTipService.BetweenShowDelay="200"
        ToolTipService.Placement="Mouse"
        ToolTipService.HorizontalOffset="12"
        ToolTipService.VerticalOffset="12">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/Design.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <DataTemplate DataType="{x:Type vm:EadViewModel}">
                <views:EadView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ReadMeViewModel}">
                <views:ReadMeView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:AgricultureDepthDamageViewModel}">
                <views:AgricultureDepthDamageView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:UpdatedCostViewModel}">
                <views:UpdatedCostView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:AnnualizerViewModel}">
                <views:AnnualizerView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:WaterDemandViewModel}">
                <views:WaterDemandView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:UdvViewModel}">
                <views:UdvView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:RecreationCapacityViewModel}">
                <views:RecreationCapacityView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:GanttViewModel}">
                <views:GanttView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:StageDamageOrganizerViewModel}">
                <views:StageDamageOrganizerView/>
            </DataTemplate>
            <DataTemplate x:Key="ActionsWideTemplate">
                <Grid HorizontalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="CalculateButton"
                            Grid.Column="0"
                            Command="{Binding CalculateCommand}"
                            Margin="0,0,12,0"
                            Height="64"
                            Visibility="{Binding IsCalculateVisible, Converter={StaticResource BoolToVisibilityConverter}}"
                            IsEnabled="{Binding IsCalculateVisible}">
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                            <TextBlock FontFamily="Segoe MDL2 Assets"
                                       Text="&#xE8EF;"
                                       Margin="0,0,8,0"
                                       FontSize="20"/>
                            <TextBlock Text="{Binding PrimaryActionLabel, Converter={StaticResource NullOrEmptyToValueConverter}, ConverterParameter=Calculate}"
                                       FontSize="16"/>
                        </StackPanel>
                    </Button>
                    <Button Grid.Column="1"
                            Command="{Binding ExportCommand}"
                            Margin="0"
                            Height="64">
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                            <TextBlock FontFamily="Segoe MDL2 Assets"
                                       Text="&#xEDE1;"
                                       Margin="0,0,8,0"
                                       FontSize="20"/>
                            <TextBlock Text="Export"
                                       FontSize="16"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </DataTemplate>

            <DataTemplate x:Key="ActionsNarrowTemplate">
                <StackPanel>
                    <Button Command="{Binding CalculateCommand}"
                            Margin="0,0,0,12"
                            Height="56"
                            Visibility="{Binding IsCalculateVisible, Converter={StaticResource BoolToVisibilityConverter}}"
                            IsEnabled="{Binding IsCalculateVisible}">
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                            <TextBlock FontFamily="Segoe MDL2 Assets"
                                       Text="&#xE8EF;"
                                       Margin="0,0,8,0"
                                       FontSize="20"/>
                            <TextBlock Text="{Binding PrimaryActionLabel, Converter={StaticResource NullOrEmptyToValueConverter}, ConverterParameter=Calculate}"
                                       FontSize="16"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ExportCommand}"
                            Margin="0"
                            Height="56">
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                            <TextBlock FontFamily="Segoe MDL2 Assets"
                                       Text="&#xEDE1;"
                                       Margin="0,0,8,0"
                                       FontSize="20"/>
                            <TextBlock Text="Export"
                                       FontSize="16"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource Brush.Navigation}"
                BorderBrush="{StaticResource Brush.NavigationBorder}"
                BorderThickness="0,0,0,1"
                Padding="12,8"
                SnapsToDevicePixels="True">
        </Border>

        <Border Grid.Row="1"
                Margin="{StaticResource Margin.Container}"
                Padding="0"
                Background="{StaticResource Brush.SurfaceAlt}"
                CornerRadius="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Style="{StaticResource Border.Hero}"
                        Margin="12"
                        Grid.Row="0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Economics Toolbox"
                                       Style="{StaticResource Text.Title}"/>
                            <TextBlock Text="Tools for common Corps planning economics tasks"
                                       Style="{StaticResource Text.Title.Small}"
                                       Foreground="#DCEBFA"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="260"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0"
                            Background="{StaticResource Brush.Navigation}"
                            BorderBrush="{StaticResource Brush.NavigationBorder}"
                            BorderThickness="0,1,1,0"
                            CornerRadius="12,0,0,12">
                        <Grid Margin="12">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="Modules"
                                       Foreground="{StaticResource Brush.NavigationText}"
                                       FontWeight="SemiBold"
                                       Margin="4,0,0,8"/>
                            <ListBox ItemsSource="{Binding Modules}"
                                     SelectedIndex="{Binding SelectedIndex}"
                                     Grid.Row="1"
                                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                                     Style="{StaticResource ListBox.Navigation}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel ToolTip="{Binding Description}">
                                            <TextBlock Text="{Binding Title}"
                                                       Foreground="{StaticResource Brush.NavigationText}"
                                                       FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>

                    <TabControl Grid.Column="1"
                                ItemsSource="{Binding Modules}"
                                SelectedIndex="{Binding SelectedIndex}"
                                Background="{StaticResource Brush.Surface}"
                                BorderThickness="0">
                        <TabControl.ItemContainerStyle>
                            <Style TargetType="TabItem">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </Style>
                        </TabControl.ItemContainerStyle>
                        <TabControl.ContentTemplate>
                            <DataTemplate>
                                <ScrollViewer x:Name="MainContentScrollViewer"
                                              Focusable="False"
                                              HorizontalScrollBarVisibility="Disabled"
                                              VerticalScrollBarVisibility="Auto"
                                              PreviewMouseWheel="MainScrollViewer_PreviewMouseWheel">
                                    <ContentControl Content="{Binding ContentViewModel}"
                                                    Margin="{StaticResource Margin.Container}"/>
                                </ScrollViewer>
                            </DataTemplate>
                        </TabControl.ContentTemplate>
                    </TabControl>
                </Grid>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Background="{StaticResource Brush.SurfaceAlt}"
                Padding="{StaticResource Padding.Section}">
            <ContentControl Content="{Binding}">
                <ContentControl.Style>
                    <Style TargetType="ContentControl">
                        <Setter Property="ContentTemplate" Value="{StaticResource ActionsNarrowTemplate}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource WidthToLayoutModeConverter}}"
                                         Value="Wide">
                                <Setter Property="ContentTemplate" Value="{StaticResource ActionsWideTemplate}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentControl.Style>
            </ContentControl>
        </Border>
    </Grid>
</Window>
